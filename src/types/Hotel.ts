export type Hotel = {
    id: number,
    name: string,
    address: string,
    description: string,
    phoneNumber: string,
    email: string,

    // rooms: string,
    // amenities: string,
    // hotelImages: string,
}